{
    "name": "regex-rules",
    "description": "Apply a combination of regexes to an input and see if it passes or fails",
    "version": "0.0.2",
    "main": "src/index.js",
    "scripts": {
        "test": "nyc mocha",
        "lint": "eslint src test",
        "lint-fix": "eslint --fix src test"
    },
    "dependencies": {},
    "devDependencies": {
        "chai": "^4.1.2",
        "eslint": "^4.9.0",
        "eslint-config-prettier": "^2.6.0",
        "eslint-plugin-prettier": "^2.3.1",
        "mocha": "^4.0.1",
        "nyc": "^11.2.1",
        "prettier": "^1.7.4"
    }
}
